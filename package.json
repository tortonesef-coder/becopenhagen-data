{
  "name": "becopenhagen-data",
  "version": "0.3.0",
  "private": true,
  "description": "Ask business questions in plain English over BeCopenhagen's data. Read only, everywhere, always.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "verify": "./scripts/verify.sh",
    "refresh": "./scripts/refresh.sh"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^2.2.0",
    "session-file-store": "^1.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
